{
  "name": "dsh-vision-3090-fix",
  "version": "0.2.0",
  "description": "Local reverse proxy plugin for DeepSeek Harness that caps images per request to 1, for self-hosted vLLM backends that reject prompts with more than one image",
  "type": "module",
  "main": "src/index.js",
  "files": [
    "src",
    "cordis.patch.yml"
  ],
  "license": "MIT",
  "keywords": [
    "dsh-plugin",
    "deepseek-harness"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "test": "node --test test/"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.2"
  },
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.2",
    "undici": "^8.10.2"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "4.0.2"
  }
}
